<!-- res/drawable/edittext_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Background color -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Border color and width -->
    <corners android:radius="4dp" /> <!-- Optional: round corners -->
</shape>
